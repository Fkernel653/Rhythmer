[project]
name = "Rhythmer"
version = "0.1.0"
description = "Rhythmer is a TUI audio downloader based on yt-dlp"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "yt-dlp",
    "textual",
    "mutagen"
]